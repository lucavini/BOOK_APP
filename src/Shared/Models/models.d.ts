export as namespace models;

export interface BooksSaled {
  id: string;
  title: string;
  date: string;
  subject: string;
  price: string;
}
export interface BooksStoraged {
  id: string;
  title: string;
  cover: string;
  subject: string;
  price: string;
}
